# ============================================
# docker-compose.yml - React Todo App
# Author: Farah Naaz
# Usage: docker-compose up -d
# ============================================

version: '3.8'

services:
  react-app:
    build: .
    container_name: react-todo-app
    ports:
      - "3000:3000"
    restart: always
